version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "8087:5000"
    env_file:
      - .env
    restart: unless-stopped
    # For a public deployment consider adding a reverse proxy (Caddy/Traefik) for TLS
